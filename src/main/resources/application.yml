micronaut:
  application:
    name: app

datasources:
  default:
    url: jdbc:postgresql://172.24.0.2:5432/attendance
    username: postgres
    password: 'admin'
    db-type: postgres
    dialect: POSTGRES
    driver-class-name: org.postgresql.Driver
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
